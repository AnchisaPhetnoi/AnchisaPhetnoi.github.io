# Security Notice

These workflows are sanitized examples intended for portfolio demonstration.

- No credentials are included.
- Credential IDs and names were removed.
- Authentication headers and sensitive values were replaced or removed.
- Workflows are disabled by default.

Always use n8n Credentials or environment variables for secrets. Never commit API keys, access tokens, cookies, passwords, webhook secrets, or production account identifiers to a public repository.
